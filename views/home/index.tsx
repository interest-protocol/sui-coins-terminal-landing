import { FC } from 'react';

import { Layout } from '@/components';

import { PartnerSection } from '../components';

const Home: FC = () => (
  <Layout>
    <PartnerSection />
  </Layout>
);

export default Home;
